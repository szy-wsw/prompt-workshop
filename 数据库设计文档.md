# 数据库设计文档

> **项目名称**: Prompt Workshop  
> **数据库类型**: Supabase PostgreSQL  
> **设计日期**: 2026-07-18  
> **状态**: 正式版  

---

## 一、数据库概述

本项目使用 **Supabase PostgreSQL** 作为后端数据库，包含 7 张核心数据表，支持用户认证、提示词管理、AI对话、互动功能等业务场景。数据库采用 UUID 作为主键，通过外键约束维护数据完整性，并启用 RLS（行级安全）策略确保数据隔离。

---

## 二、数据表设计

### 2.1 users（用户表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uuid | PRIMARY KEY, DEFAULT gen_random_uuid() | 用户唯一标识 |
| email | text | UNIQUE, NOT NULL | 用户邮箱（登录账号） |
| password | text | NOT NULL | 密码哈希值（PBKDF2-SHA512） |
| salt | text | NOT NULL | 密码加盐值 |
| nickname | text | NOT NULL | 用户昵称 |
| avatar_url | text | DEFAULT '' | 头像CDN链接 |
| avatar_path | text | DEFAULT '' | 头像在Supabase Storage的路径 |
| avatar | text | DEFAULT '' | 头像字段兼容（待清理） |
| created_at | timestamptz | DEFAULT now() | 创建时间 |
| updated_at | timestamptz | DEFAULT now() | 更新时间 |

**索引**: `idx_users_email`（隐式唯一索引）

**RLS策略**: 用户只能读取自己的信息

---

### 2.2 prompts（提示词表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uuid | PRIMARY KEY, DEFAULT gen_random_uuid() | 提示词唯一标识 |
| title | text | NOT NULL | 提示词标题 |
| content | text | NOT NULL | 提示词内容 |
| tags | text[] | DEFAULT '{}' | 标签数组 |
| visibility | text | DEFAULT 'private' | 可见性：private/public |
| author_id | uuid | NOT NULL, REFERENCES users(id) | 作者ID |
| likes_count | int | DEFAULT 0 | 点赞数（缓存字段） |
| created_at | timestamptz | DEFAULT now() | 创建时间 |
| updated_at | timestamptz | DEFAULT now() | 更新时间 |

**索引**:
- `idx_prompts_author_id` - 作者ID索引
- `idx_prompts_visibility` - 可见性索引
- `idx_prompts_created_at` - 创建时间倒序索引
- `idx_prompts_tags` - 标签GIN索引（支持数组查询）

**RLS策略**: 公开提示词所有人可读，私密提示词仅作者可读

---

### 2.3 prompt_history（提示词历史表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uuid | PRIMARY KEY, DEFAULT gen_random_uuid() | 历史记录唯一标识 |
| prompt_id | uuid | NOT NULL, REFERENCES prompts(id) ON DELETE CASCADE | 关联提示词ID |
| title | text | NOT NULL | 历史版本标题 |
| content | text | NOT NULL | 历史版本内容 |
| tags | text[] | DEFAULT '{}' | 历史版本标签 |
| visibility | text | DEFAULT 'private' | 历史版本可见性 |
| edited_by | uuid | REFERENCES users(id) ON DELETE SET NULL | 编辑者ID |
| created_at | timestamptz | DEFAULT now() | 创建时间 |

**索引**: `idx_prompt_history_prompt_id`

**RLS策略**: 仅作者可读

---

### 2.4 likes（点赞表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uuid | PRIMARY KEY, DEFAULT gen_random_uuid() | 点赞记录唯一标识 |
| prompt_id | uuid | NOT NULL, REFERENCES prompts(id) ON DELETE CASCADE | 关联提示词ID |
| user_id | uuid | NOT NULL, REFERENCES users(id) ON DELETE CASCADE | 点赞用户ID |
| created_at | timestamptz | DEFAULT now() | 创建时间 |

**唯一约束**: `unique(prompt_id, user_id)` - 防止重复点赞

**索引**: `idx_likes_prompt_user`

**RLS策略**: 仅用户自己可读

---

### 2.5 collections（收藏表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uuid | PRIMARY KEY, DEFAULT gen_random_uuid() | 收藏记录唯一标识 |
| prompt_id | uuid | NOT NULL, REFERENCES prompts(id) ON DELETE CASCADE | 关联提示词ID |
| user_id | uuid | NOT NULL, REFERENCES users(id) ON DELETE CASCADE | 收藏用户ID |
| created_at | timestamptz | DEFAULT now() | 创建时间 |

**唯一约束**: `unique(prompt_id, user_id)` - 防止重复收藏

**索引**: `idx_collections_prompt_user`

**RLS策略**: 仅用户自己可读

---

### 2.6 chat_history（聊天历史表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uuid | PRIMARY KEY, DEFAULT gen_random_uuid() | 聊天记录唯一标识 |
| user_id | uuid | NOT NULL, REFERENCES users(id) ON DELETE CASCADE | 用户ID |
| conversation_id | uuid | | 会话ID（用于关联多轮对话） |
| user_message | text | NOT NULL | 用户消息内容 |
| ai_response | text | NOT NULL | AI响应内容 |
| model | text | | 使用的AI模型名称 |
| created_at | timestamptz | DEFAULT now() | 创建时间 |

**索引**: `idx_chat_history_user_id`

**RLS策略**: 仅用户自己可读

---

### 2.7 templates（模板表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uuid | PRIMARY KEY, DEFAULT gen_random_uuid() | 模板唯一标识 |
| title | text | NOT NULL | 模板标题 |
| content | text | NOT NULL | 模板内容（含占位符） |
| tags | text[] | DEFAULT '{}' | 标签数组 |
| category | text | | 模板分类 |
| description | text | | 模板描述 |
| created_at | timestamptz | DEFAULT now() | 创建时间 |

**RLS策略**: 所有人可读

---

## 三、实体关系图（ER图）

```
┌──────────┐     1:N     ┌──────────┐
│  users   │────────────>│ prompts  │
└──────────┘             └────┬─────┘
     │                        │
     │ 1:N                    │ 1:N
     ▼                        ▼
┌───────────────┐     ┌───────────────┐
│ chat_history  │     │prompt_history │
└───────────────┘     └───────────────┘
     │                        │
     └──────────┬─────────────┘
                │
           ┌────┴────┐
           │  likes  │
           │collections│
           └─────────┘
```

**关系说明**:
- 1 用户 → N 提示词（author_id外键）
- 1 用户 → N 聊天记录（user_id外键）
- 1 提示词 → N 历史版本（prompt_id外键）
- 1 提示词 → N 点赞记录（prompt_id外键）
- 1 提示词 → N 收藏记录（prompt_id外键）

---

## 四、存储配置

### 4.1 avatars Bucket

| 配置项 | 值 |
|--------|------|
| Bucket名称 | avatars |
| 访问权限 | Public |
| 存储路径 | `avatars/{user_id}/{filename}` |
| 支持格式 | jpg, png, webp |
| 文件大小限制 | 2MB |

### 4.2 CDN访问格式

```
https://<supabase-project-id>.supabase.co/storage/v1/object/public/avatars/{user_id}/{filename}
```

---

## 五、安全设计

### 5.1 行级安全（RLS）

| 表名 | 策略名称 | 权限 | 规则 |
|------|----------|------|------|
| users | users read own | SELECT | auth.uid() = id |
| prompts | prompts read public or own | SELECT | visibility = 'public' OR auth.uid() = author_id |
| prompt_history | prompt_history read own | SELECT | auth.uid() = edited_by |
| likes | likes read own | SELECT | auth.uid() = user_id |
| collections | collections read own | SELECT | auth.uid() = user_id |
| chat_history | chat_history read own | SELECT | auth.uid() = user_id |
| templates | templates read all | SELECT | true |

### 5.2 注意事项

> **重要**: 当前项目使用 `service_role` 客户端绕过 RLS，RLS 策略作为安全兜底。建议后续改为使用 `createServerClient` + 用户 JWT Token，使 RLS 策略生效。

---

## 六、数据库初始化

### 6.1 执行步骤

1. 在 Supabase Dashboard 中创建项目
2. 进入 SQL Editor
3. 执行 `frontend/supabase/schema.sql` 脚本

### 6.2 环境变量配置

| 变量名 | 说明 |
|--------|------|
| NEXT_PUBLIC_SUPABASE_URL | Supabase项目URL |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | Supabase匿名密钥 |
| SUPABASE_SERVICE_ROLE_KEY | Supabase服务端密钥（用于绕过RLS） |

---

## 七、待优化项

| 优先级 | 问题 | 说明 |
|--------|------|------|
| P0 | RLS策略未生效 | 当前使用service_role绕过RLS |
| P1 | avatar字段冗余 | users表同时存在avatar/avatar_url/avatar_path |
| P1 | 缺少软删除字段 | prompts表缺少deleted_at字段 |
| P2 | 缺少全文搜索索引 | prompts表可增加content字段的tsvector索引 |

---

> **文档版本**: v1.0  
> **生成时间**: 2026-07-18  
> **备注**:http://szy050604.top
