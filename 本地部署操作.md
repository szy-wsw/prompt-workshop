# 本地部署操作手册

> **项目名称**: Prompt Workshop  
> **技术栈**: Next.js 16 + Supabase + SiliconFlow AI  
> **编写日期**: 2026-07-18  

---

## 一、环境要求

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| Node.js | >= 18.0.0 | 建议使用 LTS 版本 |
| npm | >= 9.0.0 | 包管理器 |
| Git | >= 2.0.0 | 版本控制 |

---

## 二、部署步骤

### 2.1 克隆项目

```bash
git clone https://github.com/szy-wsw/prompt-workshop.git
cd prompt-workshop/frontend
```

---

### 2.2 安装依赖

```bash
npm install
```

---

### 2.3 配置环境变量

创建 `.env.local` 文件：

```bash
cp .env.example .env.local
```

编辑 `.env.local`，填入以下配置：

```env
# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# JWT 密钥（必须配置）
JWT_SECRET=your-secret-jwt-key

# AI 服务配置（硅基流动）
SILICONFLOW_API_KEY=your-siliconflow-api-key
FREE_MODEL_ID=qwen/Qwen2.5-7B-Instruct

# 应用 URL（用于回调和跨域）
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**获取方式**：
1. Supabase 配置：登录 [Supabase Dashboard](https://supabase.com/dashboard) → 进入项目 → 设置 → API
2. SILICONFLOW_API_KEY：登录 [硅基流动平台](https://cloud.siliconflow.cn/) 获取免费 API Key

---

### 2.4 初始化数据库

在 Supabase Dashboard 中执行数据库初始化脚本：

1. 进入 **SQL Editor**
2. 新建查询
3. 粘贴 `frontend/supabase/schema.sql` 内容
4. 点击 **Run** 执行

**脚本功能**：
- 创建 7 张数据表（users, prompts, prompt_history, likes, collections, chat_history, templates）
- 创建必要索引
- 启用 RLS 行级安全策略
- 创建 avatars 存储桶
- 插入示例模板数据

---

### 2.5 启动开发服务器

```bash
npm run dev
```

访问地址：`http://localhost:3000`

---

### 2.6 验证服务

启动后验证以下功能：

| 功能 | 验证方法 |
|------|----------|
| 用户注册 | 访问 `/register` 注册新用户 |
| 用户登录 | 访问 `/login` 登录 |
| AI 对话 | 登录后访问 `/ai-workspace` 发送消息 |
| 提示词管理 | 访问 `/prompt` 创建/编辑/删除提示词 |
| 头像上传 | 访问 `/profile` 上传头像 |
| 点赞/收藏 | 在提示词卡片上点击点赞/收藏按钮 |

---

## 三、项目构建

### 3.1 构建生产版本

```bash
npm run build
```

构建成功后，产物位于 `.next/` 目录。

### 3.2 启动生产服务器

```bash
npm start
```

---

## 四、常见问题

### 4.1 Supabase 连接失败

**错误信息**:
```
Failed to connect to Supabase
```

**解决方案**:
1. 检查 `.env.local` 中的 `NEXT_PUBLIC_SUPABASE_URL` 和 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 是否正确
2. 确认网络可以访问 Supabase（国内网络可能需要代理）
3. 检查 Supabase 项目是否已创建且状态正常

---

### 4.2 AI 接口返回 503

**错误信息**:
```
AI服务未配置，请联系管理员设置 AI_API_KEY
```

**解决方案**:
1. 检查 `.env.local` 中的 `AI_API_KEY`（或 `SILICONFLOW_API_KEY`）是否正确
2. 确认硅基流动(SiliconFlow)账号已注册且 API Key 有效
3. 检查网络是否可以访问硅基流动 API (https://api.siliconflow.cn)
4. 确保账号已完成实名认证并充值，免费额度过期后需要付费使用

---

### 4.3 头像上传失败

**错误信息**:
```
Avatar upload failed
```

**解决方案**:
1. 检查 Supabase Storage 是否已创建 `avatars` bucket
2. 确认 bucket 权限为 Public
3. 检查文件格式是否为 jpg/png/webp
4. 检查文件大小是否超过 2MB

---

### 4.4 JWT 验证失败

**错误信息**:
```
Invalid token
```

**解决方案**:
1. 检查 `.env.local` 中的 `JWT_SECRET` 是否配置
2. 确认前后端使用相同的 JWT_SECRET
3. 清除浏览器缓存后重新登录

---

## 五、目录结构说明

```
frontend/
├── app/                    # Next.js 页面路由
│   ├── api/                # API 路由（后端逻辑）
│   ├── ai-workspace/       # AI 对话页面
│   ├── collection/         # 收藏页面
│   ├── forum/              # 公共论坛页面
│   ├── login/              # 登录页面
│   ├── profile/            # 个人中心页面
│   ├── prompt/             # 提示词管理页面
│   ├── register/           # 注册页面
│   ├── templates/          # 模板库页面
│   └── page.tsx            # 首页
├── components/             # React 组件
├── hooks/                  # 自定义 Hooks
├── lib/                    # 工具函数
│   ├── password.ts         # 密码哈希工具
│   ├── supabase-server.ts  # Supabase 服务端客户端
│   └── auth.tsx            # 认证上下文
├── supabase/               # Supabase 配置
│   └── schema.sql          # 数据库初始化脚本
├── tests/                  # 测试脚本
├── types/                  # TypeScript 类型定义
└── next.config.ts          # Next.js 配置
```

---

## 六、测试命令

### 6.1 运行自动化测试

```bash
npm run test
```

### 6.2 检查 Supabase 连接

```bash
npm run check-supabase
```

---

## 七、部署到 Vercel（待域名解析后）

### 7.1 创建 Vercel 项目

1. 访问 [Vercel Dashboard](https://vercel.com/)
2. 点击 **Add New Project**
3. 选择 GitHub 仓库 `szy-wsw/prompt-workshop`
4. 配置构建命令：`npm run build`
5. 配置输出目录：`.next`

### 7.2 添加环境变量

在 Vercel 项目设置中添加以下环境变量：

| 变量名 | 值 |
|--------|------|
| NEXT_PUBLIC_SUPABASE_URL | Supabase URL |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | Supabase Anon Key |
| SUPABASE_SERVICE_ROLE_KEY | Supabase Service Role Key |
| JWT_SECRET | JWT 密钥 |
| SILICONFLOW_API_KEY | 硅基流动 API Key |
| FREE_MODEL_ID | qwen/Qwen2.5-7B-Instruct |
| NEXT_PUBLIC_APP_URL | 部署后的域名 |

### 7.3 配置自定义域名

1. 在 Vercel 项目设置中添加自定义域名
2. 在域名服务商处配置 DNS 解析（CNAME 记录）
3. 等待 SSL 证书自动签发

---

> **文档版本**: v1.0  
> **生成时间**: 2026-07-18  
> **备注**: 本文档预留了域名替换位置，待域名解析完成后更新部署地址。
