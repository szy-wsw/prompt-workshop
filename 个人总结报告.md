# 个人实训总结报告

本次实训项目是开发一个基于 Next.js + Supabase 的 AI 提示词管理平台，涵盖用户认证、提示词管理、AI 流式对话、点赞收藏、头像上传等功能。整个开发过程中，我从需求分析、架构设计到代码实现，逐步完成了全栈项目的构建，收获颇丰。

技术层面，我选用 Next.js 16 App Router 作为前端框架，结合 TypeScript 保障类型安全，后端使用 Supabase PostgreSQL 数据库和 Storage 存储服务，AI 接入硅基流动的 Qwen2.5-7B-Instruct 免费大模型。开发过程中遇到了不少挑战，最突出的是海外网站访问困难——国内网络访问 GitHub、Supabase、Vercel 等海外服务时经常超时或连接失败，严重影响了开发和调试效率。为了解决这个问题，我通过本地测试验证功能，将部署流程推后，先把前期准备工作做足，等域名可用时直接替换即可上线。

域名方面也遇到了实名认证和 DNS 解析的难题。由于域名需要实名认证才能使用，加上国内访问 Vercel 域名超时，后续还考虑过切换到 Cloudflare Pages、Netlify 等多个备选方案，最终决定等域名解析完成后统一部署。这段经历让我深刻体会到了海外服务在国内使用的实际限制，也学会了如何在受限环境下合理规划开发节奏。

项目迭代过程中，我还经历了从 MongoDB 到腾讯云 TCB 再到 Supabase 的多次技术栈迁移，每次迁移都让我对不同后端方案有了更深入的理解。同时，通过 AI Code Review 发现了 JWT 密钥安全、RLS 行级安全、类型安全等问题，进一步提升了代码质量。

本次实训不仅锻炼了我的全栈开发能力，更重要的是培养了我解决实际问题的能力和面对困难时的耐心与韧性。
