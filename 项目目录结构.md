# 项目目录结构

> **项目名称**: Prompt Workshop  
> **技术栈**: Next.js 16 + Supabase + SiliconFlow AI  
> **编写日期**: 2026-07-18  

---

## 一、项目根目录

```
prompt-workshop/
├── frontend/                # Next.js 前端应用（核心）
├── docs/                    # 项目文档
├── .gitignore               # Git 忽略配置
├── README.md                # 项目说明文档
└── package.json             # 根目录依赖（可选）
```

---

## 二、frontend/ 目录（核心）

```
frontend/
├── app/                     # Next.js App Router 页面路由
│   ├── api/                 # API 路由（后端逻辑）
│   │   ├── ai/              # AI 状态接口
│   │   │   └── status/
│   │   │       └── route.ts
│   │   ├── chat/            # AI 对话接口
│   │   │   └── route.ts
│   │   ├── chat-history/    # 聊天历史接口
│   │   │   └── route.ts
│   │   ├── collections/     # 收藏接口
│   │   │   ├── check/
│   │   │   │   └── route.ts
│   │   │   └── route.ts
│   │   ├── likes/           # 点赞接口
│   │   │   ├── check/
│   │   │   │   └── route.ts
│   │   │   └── route.ts
│   │   ├── login/           # 登录接口
│   │   │   └── route.ts
│   │   ├── profile/         # 用户信息接口
│   │   │   └── route.ts
│   │   ├── prompts/         # 提示词接口
│   │   │   ├── [id]/
│   │   │   │   ├── history/
│   │   │   │   │   └── route.ts
│   │   │   │   └── route.ts
│   │   │   └── route.ts
│   │   ├── signup/          # 注册接口
│   │   │   └── route.ts
│   │   ├── templates/       # 模板接口
│   │   │   └── route.ts
│   │   └── users/           # 用户统计接口
│   │       ├── ranking/
│   │       │   └── route.ts
│   │       └── stats/
│   │           └── route.ts
│   ├── ai-workspace/        # AI 对话页面
│   │   └── page.tsx
│   ├── collection/          # 收藏页面
│   │   └── page.tsx
│   ├── forum/               # 公共论坛页面
│   │   └── page.tsx
│   ├── login/               # 登录页面
│   │   └── page.tsx
│   ├── profile/             # 个人中心页面
│   │   └── page.tsx
│   ├── prompt/              # 提示词管理页面
│   │   └── page.tsx
│   ├── register/            # 注册页面
│   │   └── page.tsx
│   ├── templates/           # 模板库页面
│   │   └── page.tsx
│   ├── ThemeProvider.tsx    # 主题提供者
│   ├── ThemeToggle.tsx      # 主题切换组件
│   ├── favicon.ico          # 网站图标
│   ├── globals.css          # 全局样式
│   ├── layout.tsx           # 全局布局
│   ├── not-found.tsx        # 404 页面
│   ├── page.tsx             # 首页
│   ├── theme.ts             # 主题配置
│   └── useTheme.ts          # 主题 Hook
├── components/              # React 公共组件
│   ├── ConfirmModal.tsx     # 确认弹窗
│   ├── Empty.tsx            # 空状态组件
│   ├── FloatingButton.tsx   # 浮动按钮
│   ├── HistoryModal.tsx     # 历史记录弹窗
│   ├── LoginPopup.tsx       # 登录弹窗
│   ├── Modal.tsx            # 基础弹窗
│   ├── Navbar.tsx           # 导航栏
│   ├── PromptCard.tsx       # 提示词卡片
│   ├── PromptForm.tsx       # 提示词表单
│   ├── Skeleton.tsx         # 骨架屏
│   ├── ThemeToggle.tsx      # 主题切换
│   ├── Toast.tsx            # 消息提示
│   └── UserMenu.tsx         # 用户菜单
├── hooks/                   # 自定义 Hooks
│   └── useTheme.ts          # 主题 Hook
├── lib/                     # 工具函数库
│   ├── auth.tsx             # 认证上下文
│   ├── password.ts          # 密码哈希工具
│   ├── supabase-server.ts   # Supabase 服务端客户端
│   ├── supabase.ts          # Supabase 前端客户端
│   ├── theme.ts             # 主题工具
│   ├── utils.ts             # 通用工具
│   └── validation.ts        # 表单验证
├── public/                  # 静态资源
│   ├── file.svg
│   ├── globe.svg
│   ├── next.svg
│   ├── vercel.svg
│   └── window.svg
├── scripts/                 # 脚本工具
│   ├── check-supabase.mjs   # Supabase 连接检查
│   └── create_indexes.sql   # 索引创建脚本
├── supabase/                # Supabase 配置
│   ├── cors-policy.json     # CORS 策略配置
│   └── schema.sql           # 数据库初始化脚本
├── tests/                   # 测试脚本
│   └── run-tests.mjs        # 自动化测试
├── types/                   # TypeScript 类型定义
│   ├── like.ts              # 点赞类型
│   ├── prompt.ts            # 提示词类型
│   └── user.ts              # 用户类型
├── .env.local               # 本地环境变量
├── .gitignore               # Git 忽略配置
├── eslint.config.mjs        # ESLint 配置
├── middleware.ts            # Next.js 中间件
├── next-env.d.ts            # Next.js 类型声明
├── next.config.ts           # Next.js 配置
├── package-lock.json        # 依赖锁定文件
├── package.json             # 依赖配置
├── postcss.config.mjs       # PostCSS 配置
├── tsconfig.json            # TypeScript 配置
├── tsconfig.tsbuildinfo     # TypeScript 构建信息
└── vercel.json              # Vercel 部署配置
```

---

## 三、目录职责说明

### 3.1 app/

Next.js App Router 的核心目录，包含页面路由和 API 路由：

| 目录 | 职责 |
|------|------|
| `app/api/` | 后端 API 接口，处理所有数据请求 |
| `app/ai-workspace/` | AI 对话页面（非流式响应，包含单条测试、多轮对话、润色优化） |
| `app/collection/` | 用户收藏页面 |
| `app/forum/` | 公共论坛页面，展示公开提示词 |
| `app/login/` | 用户登录页面 |
| `app/profile/` | 个人中心页面，包含头像上传 |
| `app/prompt/` | 提示词管理页面（CRUD） |
| `app/register/` | 用户注册页面 |
| `app/templates/` | 模板库页面，预设提示词模板 |

### 3.2 components/

React 公共组件，可复用的 UI 元素：

| 组件 | 职责 |
|------|------|
| `Navbar.tsx` | 顶部导航栏 |
| `PromptCard.tsx` | 提示词卡片展示 |
| `PromptForm.tsx` | 提示词创建/编辑表单 |
| `FloatingButton.tsx` | 浮动操作按钮 |
| `Toast.tsx` | 全局消息提示 |
| `Modal.tsx` | 基础弹窗组件 |
| `ConfirmModal.tsx` | 确认弹窗 |

### 3.3 lib/

工具函数库，封装通用逻辑：

| 文件 | 职责 |
|------|------|
| `auth.tsx` | 用户认证状态管理（React Context） |
| `password.ts` | 密码哈希与验证（PBKDF2-SHA512） |
| `supabase-server.ts` | Supabase 服务端操作封装 |
| `supabase.ts` | Supabase 前端请求封装 |
| `utils.ts` | 通用工具函数 |
| `validation.ts` | 表单验证规则 |

### 3.4 supabase/

Supabase 数据库配置：

| 文件 | 职责 |
|------|------|
| `schema.sql` | 数据库表结构、索引、RLS 策略 |
| `cors-policy.json` | CORS 跨域策略配置 |

---

## 四、技术栈说明

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 前端框架 | Next.js | 16 | App Router 模式 |
| 语言 | TypeScript | 5 | 类型安全 |
| UI | React | 19 | 用户界面组件 |
| 样式 | CSS Modules + Tailwind | - | 样式方案 |
| 数据库 | Supabase PostgreSQL | - | 云数据库 |
| 存储 | Supabase Storage | - | 头像存储 |
| AI | SiliconFlow | - | Qwen2.5-7B-Instruct |
| 认证 | JWT | - | 自定义认证 |
| 部署 | Vercel | - | 线上部署 |

---

## 五、关键文件说明

| 文件 | 说明 | 重要性 |
|------|------|--------|
| `app/layout.tsx` | 全局布局，注入 ThemeProvider | 高 |
| `lib/supabase-server.ts` | 数据库操作封装，所有 API 路由依赖 | 高 |
| `lib/password.ts` | 密码安全处理，防止时序攻击 | 高 |
| `lib/auth.tsx` | 认证上下文，前端状态管理 | 高 |
| `supabase/schema.sql` | 数据库初始化，包含 RLS 策略 | 高 |
| `next.config.ts` | Next.js 配置，包含 Vercel 适配 | 中 |
| `vercel.json` | Vercel 部署配置，重写规则 | 中 |

---

## 六、数据流说明

```
用户请求
    ↓
Next.js App Router
    ↓
API Route (app/api/*)
    ↓
lib/supabase-server.ts
    ↓
Supabase PostgreSQL
    ↓
返回数据
```

---

> **文档版本**: v1.0  
> **生成时间**: 2026-07-18  
> **备注**: 本文档预留了域名替换位置，待域名解析完成后更新相关配置。
